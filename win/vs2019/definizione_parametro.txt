1. definisco parametri necessari al plugin nella classe principale, definendo una variabile es float gain;
2. imposto anche il numero di parametri supportati quando chiamo il costruttore di AudioEffectX AudioEffectX(audioMaster, 0, 1)	// n program, n parameters
3. funzioni setParameter(), getParameter(), getParameterLabel(), getParameterDisplay(), getParameterName()
	- queste funzioni sono definite nella SDK VST audioeffect.h
	- vedi audioeffect.h --> Programs and Persistence
	- questi metodi sono chiamati dalla DAW per ricavare le informazioni come il valore del parametro, il nome del parametro e l'unità di misura
4. definisco questi metodi nella classe principale MisEfx in MisEfx.h specificando override dopo i parametri mi assicuro che sto sovrascrivendo la funzione della classe padre 

	************* ESEMPIO **********************
	void setParameter(VstInt32 index, float value) override;	///< Called when a parameter changed
	float getParameter(VstInt32 index) override;	///< Return the value of the parameter with \e index
	void getParameterLabel(VstInt32 index, char* label) override;	///< Stuff \e label with the units in which parameter \e index is displayed (i.e. "sec", "dB", "type", etc...). Limited to #kVstMaxParamStrLen.
	void getParameterDisplay(VstInt32 index, char* text) override;	///< Stuff \e text with a string representation ("0.5", "-3", "PLATE", etc...) of the value of parameter \e index. Limited to #kVstMaxParamStrLen.
	void getParameterName(VstInt32 index, char* text) override;    ///< Stuff \e text with the name ("Time", "Gain", "RoomType", etc...) of parameter \e index. Limited to #kVstMaxParamStrLen.
	********************************************
	
5. implemento i metodi in MisEfx in MisEfx.cpp
	setParameter() verrà chiamata dall'host per settare il parametro index all'interno del plugin. Qui assegno il valore ricevuto alla variabile gain.
	getParameter() verrà chiamata dall'host per conoscere il valore del parametro index. 
	getParameterLabel(), getParameterDisplay(), getParameterName() ritornano void, ma l'host ci passa, insieme all'indice del parametro, un puntatore alla variabile in cui scrivere il valore

	per scrivere queste string utilizzo la funzione di utility vst_strncpy, che scrive in modo safe, troncando se eccede la lunghezza massima, la stringa passata sulla variabile di cui abbiamo ricevuto il puntatore kVstMaxParamStrLen è la costante che definisce la lunghezza massima

6. aggiungo parametro bypass ed uso switch per definire il comportamento in base al valore di index nelle funzioni (index 0 --> primo parametro, index 1 --> secondo parametro)
	utilizzo un enum Params definita in MisEfx.h per associare dei valori mnemonici agli interi

7. cambio scala gain da valore normalizzato, per avere la possibilità di fornire un guadagno. Utilizzo le funzioni definite in MisEfxUtils.cpp e le costanti impostate in MisEfx.cpp tramite l'istruzione #define
I VALORI DEI PARAMETRI SONO FLOAT (da 0 a 1 in virgola mobile)